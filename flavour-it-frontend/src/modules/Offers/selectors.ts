// import { IApplicationState } from '../../store/types';
// import { createSelector } from 'reselect';

// const offersSelector = (state: IApplicationState) => state.offers;

// export const getOffersListSelector = createSelector(offersSelector, offers => offers.list);
// export const getOffersRequestingSelector = createSelector(offersSelector, offers => offers.requesting);
// export const getOffersErrorSelector = createSelector(offersSelector, offers => offers.error);
// export const getOffersTotalCount = createSelector(offersSelector, offers => offers.totalCount);
// export const getOffersTotalPages = createSelector(offersSelector, offers => offers.totalPages);
export {};
